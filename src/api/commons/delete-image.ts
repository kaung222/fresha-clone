import { ApiClient } from "../ApiClient";

// export const DeleteImage = async (imageId: string): Promise<void> => {
//   const { data } = await ApiClient.delete("files/" + imageId).then(
//     (res) => res.data
//   );
// };
