import { BlogList } from '@/components/pages/clinic-blog'
import React from 'react'

type Props = {}

const page = (props: Props) => {
    return (
        <div>
            <BlogList />
        </div>
    )
}

export default page