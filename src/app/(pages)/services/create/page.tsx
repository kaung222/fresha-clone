import { ServiceCreate } from "@/components/pages/services";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <ServiceCreate />
    </div>
  );
};

export default page;
