import { ServiceList } from "@/components/pages/services";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <ServiceList />
    </div>
  );
};

export default page;
